import React from 'react';
import './Header.css';

export const Header = props => (
  <header className="header">
    <h1 className="header__text">Kandy Korner</h1>
    <p className="header__slogan">If we had any more candy, we'd be Voltaire, cause we'd be Candide, (like, "candied"), amirite??</p>
  </header>
);